/** @type {import('next').NextConfig} */
const nextConfig = {
  // Vercel runs Next.js natively — no static-export or basePath needed.
  // The app is served at the domain root.
};

export default nextConfig;
